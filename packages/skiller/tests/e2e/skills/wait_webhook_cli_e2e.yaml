name: wait_webhook_cli_e2e
description: "CLI e2e for wait_webhook"
version: "0.1"
start: wait_signal

inputs:
  key: string

steps:
  - wait_webhook: wait_signal
    webhook: test
    key: "{{inputs.key}}"
    next: done

  - notify: done
    message: resumed from webhook
