name: http_mcp_test
description: "Minimal MCP test over streamable-http"
version: "0.1"

inputs:
  mcp_url: string

mcp:
  - name: test-mcp
    transport: streamable-http
    url: "{{inputs.mcp_url}}"

steps:
  - id: start
    type: mcp
    mcp: test-mcp
    tool: ping
    args: {}
