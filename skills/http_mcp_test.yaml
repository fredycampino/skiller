name: http_mcp_test
description: "Minimal MCP test over streamable-http"
version: "0.1"
start: ping_server

inputs:
  mcp_url: string

mcp:
  - name: test-mcp
    transport: streamable-http
    url: "{{inputs.mcp_url}}"

steps:
  - mcp: ping_server
    server: test-mcp
    tool: ping
    args: {}
