name: wait_input_test
description: "Minimal internal skill for wait_input testing"
version: "0.1"
start: ask_user

inputs: {}

steps:
  - wait_input: ask_user
    prompt: "Write a short summary"
    next: done

  - notify: done
    message: '{{output_value("ask_user").payload.text}}'
