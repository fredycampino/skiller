name: wait_input_cli_e2e
description: "CLI e2e for wait_input"
version: "0.1"

inputs: {}

steps:
  - id: start
    type: wait_input
    prompt: "Write a short summary"
    next: done

  - id: done
    type: notify
    message: "{{results.start.payload.text}}"
